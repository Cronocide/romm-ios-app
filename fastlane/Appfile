app_identifier('de.ilyashallak.rommanagerplus')

# Keep team values from project settings for automatic signing resolution.
itc_team_id('8J69P655GN')
team_id('8J69P655GN')

# Optional fallback when not using App Store Connect API key authentication.
apple_id(ENV['FASTLANE_APPLE_ID']) if ENV['FASTLANE_APPLE_ID'].to_s.strip != ''
